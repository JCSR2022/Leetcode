import string

class Solution:
    def isValid(self, word: str) -> bool:

        #best is use re but i guess is not valid

        # if len(word)<3:
        #     return False

        # if not all([ ch in string.ascii_letters+string.digits for ch in word]):          
        #     return False

        # consonant = 0
        # vowels = 0
        # for ch in word:
        #     vowels += 1 if ch in 'aeiouAEIOU' else 0
        #     consonant += 1 if ch not in 'aeiouAEIOU0123456789' else 0
        # if consonant == 0 or vowels == 0:
        #     return False
        

        # return True 

#-------------------------------------------------------------

        
        
